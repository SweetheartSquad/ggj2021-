import 'canvas-toBlob';
import 'preact';
import { render } from 'preact';

function App() {
	return (
		<main>
			<h1>TODO: title</h1>
			<section>TODO: the whole game</section>
		</main>
	);
}

render(<App />, document.body);
